# 2026-03-01 — Rust Rewrite Session (Friday night → Saturday morning)

## Completed

### M0a — Oikos TS Migration
- PR #355: TS path migration across 50+ files (Claude Code) — merged
- PR #356: Oikos cascade (cascade.ts, bootstrap, config YAML) — merged
- PR #359: Runtime paths.ts rewrite (Claude Code) — reviewed + merged
- Migration script at `scripts/migrate-to-oikos.sh` — Alice runs when near PC

### M0b — Rust Foundation
- PR #358: koina + taxis crates — merged
  - koina: NousId, SessionId, TurnId, ToolName newtypes, snafu errors, tracing init
  - taxis: Oikos paths, three-tier cascade (nous → shared → theke)

### M1.1 — Mneme SQLite Sessions
- SessionStore with rusqlite (WAL mode, prepared statement caching)
- Wire-compatible with existing TS sessions.db
- Sessions, messages, usage, distillations, agent notes

### M1.2a — CozoDB Validation Gate
- Standalone bench binary — all 5 tests pass
- Relations (1000 inserts, filtered query), HNSW (500 vectors, KNN 12ms),
  Graph (200 entities, 2-hop traversal), Concurrent R/W, Bi-temporal queries

### M1.2 — Knowledge Types + Schema
- Fact (bi-temporal, epistemic tiers), Entity, Relationship, EmbeddedChunk
- CozoDB Datalog DDL templates and query templates
- Feature-gated storage backends

### M1.3 — Embedding Provider
- EmbeddingProvider trait (Send + Sync)
- MockEmbeddingProvider for testing
- EmbeddingConfig + factory

### M1.4 — Recall Engine
- 6-factor scoring: vector similarity, recency, relevance, epistemic tier,
  relationship proximity, access frequency
- RecallWeights configurable per-nous
- 22 tests covering each factor individually + composite

### M1.5 — Hermeneus LLM Provider
- LlmProvider trait (complete, supported_models, estimate_tokens)
- Provider-agnostic types: CompletionRequest/Response, Message, Content,
  ContentBlock, ToolDefinition, ThinkingConfig, Usage
- ProviderRegistry for model→provider routing

### M2.1 — Nous Pipeline Skeleton
- SessionState, SessionManager, distillation threshold
- PipelineContext, PipelineMessage, GuardResult
- LoopDetector (tool call dedup)
- TurnResult, ToolCall, TurnUsage, InteractionSignal

### PRs Reviewed
- #357: Dependabot pypdf security patch — merged
- #359: Claude Code oikos path migration — reviewed, tested (77/77), merged

## Key Decision: CozoDB Deferred
**Decision:** Keep rusqlite for sessions, defer CozoDB unification.
**Reason:** Three upstream bugs in CozoDB 0.7.6:
1. Unconditional `rayon::spawn` in lib.rs (not behind feature flag)
2. `graph_builder` crate broken with rayon 1.10 (IntoIter/Iter mismatch)
3. `nalgebra` type resolution failures (OMatrix, Dynamic, U1)

Attempted `feat/cozo-unified` branch — got 40+ compile errors after vendor patching.
Reverted to main. Architecture supports clean swap when CozoDB 0.8 ships.

## Final State
- 6 Rust crates (koina, taxis, mneme, hermeneus, nous, mneme-bench)
- 118 workspace tests, all passing
- ~7,500 lines of Rust
- Clean clippy (pedantic + deny dbg/todo/unimplemented)
- Edition 2024, workspace lints

## Pending
- Run `scripts/migrate-to-oikos.sh` on live server (Alice, when near PC)
- CozoDB 0.8 release tracking
- M2.2+ (context bootstrap, history loading, execute stage)
